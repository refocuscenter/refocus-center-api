#generated with ts-to-openapi

components:
  paginationParams:
    page:
        in: query
        name: page
        schema:
          type: integer
          example: 0
        description: page (start at 0)
    limit:
        in: query
        name: limit
        schema:
          type: integer
          example: 20
        description: limit of elements per page
        
  responses:
    200:
      description: Operation performed successfully!

definitions:
  object:
    type: object
